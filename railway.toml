# Railway deployment configuration for AgentRail.
# https://docs.railway.com/reference/config-as-code

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
